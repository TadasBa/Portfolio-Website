import type { SocialLink } from "../types/content";

export const siteConfig: {
  email: string;
  name: string;
  role: string;
  shortName: string;
  summary: string;
};

export const socialLinks: SocialLink[];
